{
  "extends": ["next", "next/core-web-vitals", "eslint:recommended"],
  "globals": {
    "React": "readonly"
  },
  "rules": {
    "no-unused-vars": [1, { "args": "after-used", "argsIgnorePattern": "^_" }]
    // to disable an eslint rule globally, add the rule name to the ignore list:
    // eg:
    // "no-unused-vars": 0,
  }
}
